import 'package:flutter/material.dart';

class QuantumRadius {
  static const double sm = 16.0;
  static const double md = 20.0;
  static const double lg = 28.0;
  static const double xl = 36.0;
  static const double capsule = 999.0;

  static const BorderRadius borderSm = BorderRadius.all(Radius.circular(16.0));
  static const BorderRadius borderMd = BorderRadius.all(Radius.circular(20.0));
  static const BorderRadius borderLg = BorderRadius.all(Radius.circular(28.0));
  static const BorderRadius borderXl = BorderRadius.all(Radius.circular(36.0));
  static const BorderRadius borderCapsule = BorderRadius.all(Radius.circular(999.0));
}
